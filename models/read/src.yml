version: 2

sources:
  - name: raw_data
    tables:
      - name: area1
        description: "Raw input data"
        meta:
          external_location: "data/*.parquet"
        columns:
          - name: Z
            tests:
              - not_null
